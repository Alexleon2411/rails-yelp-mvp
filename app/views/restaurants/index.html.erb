<h2>Here are your restaurant</h2>
<% @restaurants.each do |restaurant| %>
  <li class="list-group-item m-3 border">
  <%= link_to restaurant.name, restaurant_path(restaurant), class: "text-decoration-none"%>
  </li>

  <% end %>
  <%= link_to "create a new restaurant", new_restaurant_path, class: "btn btn-warning"%>
